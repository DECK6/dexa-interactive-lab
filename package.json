{
  "name": "dexa-interactive-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "models": "node scripts/fetch-models.mjs",
    "dev": "bun run models && vite",
    "build": "bun run models && tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "bun test tests/unit",
    "test:e2e": "playwright test",
    "deploy": "bash scripts/deploy.sh"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.22",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/bun": "^1.3.14",
    "@types/three": "^0.185.3",
    "typescript": "^5.7.0",
    "vite": "^6.3.0"
  }
}
